kind: PersistentVolume
apiVersion: v1
metadata:
  name: makaba-reposter-rabbitmq-staging-pv
  labels:
    type: local
    app: makaba-reposter
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/home/wmw/git/blossom-bot/reposter/data/rmqdata-k8s-staging"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: makaba-reposter-staging
  name: makaba-reposter-rabbitmq-staging-pvc
  labels:
    app: makaba-reposter
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
